# SmartCatalog Environment Configuration

# Database Configuration
# ----------------------
# For LOCAL development: Use individual DB settings (DB_HOST, DB_USERNAME, etc.)
# For PRODUCTION: Use DATABASE_URL pointing to Supabase or managed PostgreSQL
# For CI/Testing: DATABASE_URL is automatically configured by GitHub Actions
#
# IMPORTANT: In local development, do NOT set DATABASE_URL if you have .env file
# loaded. Tests will automatically ignore DATABASE_URL to prevent accidentally
# connecting to production. In CI, DATABASE_URL is required and points to localhost.

DB_HOST=db
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_PORT=5432

# Gemini AI (Google AI Studio)
# Get your free API key at: https://aistudio.google.com/
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.0-flash
GEMINI_EMBEDDING_MODEL=text-embedding-004

# Rails
# Note: RAILS_ENV is intentionally not set here to allow tests to use 'test' environment
# Rails defaults to 'development' when not specified
RAILS_LOG_LEVEL=debug
